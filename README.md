# 🤖 AI Помощник Бухгалтера Молдовы

Интеллектуальная система для обработки и анализа бухгалтерских документов в Молдове с использованием искусственного интеллекта.

## 📋 Структура проекта

Проект реализован согласно следующему сценарию:

```
/
├── 📄 Загрузить документ
│   └── 🔍 Распознать
├── ✅ Проверка и исправление
│   └── ✏️ Редактировать → 📥 Скачать
├── 📁 Архив
│   └── 🔍 Поиск, 📊 Экспорт, 📈 Графики
```

## 🚀 Основные возможности

### 1. 📄 Загрузить документ
- **Drag & Drop** загрузка файлов
- Поддержка форматов: PDF, JPG, PNG, TIFF
- Автоматическое распознавание типа документа
- Извлечение данных с помощью OCR и AI
- Максимальный размер файла: 50MB

### 2. ✅ Проверка и исправление
- Просмотр распознанных документов
- Редактирование извлеченных данных
- Валидация по стандартам Молдовы
- Скачивание обработанных документов

### 3. 📁 Архив
- **Поиск** документов по различным критериям
- **Экспорт** в форматах JSON, Excel, CSV
- **Графики** и аналитика
- Фильтрация по типу, дате, статусу

## 🛠 Технологии

- **Backend**: FastAPI, Python 3.11+
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **AI/ML**: OpenAI GPT-4, Tesseract OCR
- **Database**: SQLite
- **Document Processing**: PyPDF2, Pillow, pandas

## 📦 Установка и запуск

### Предварительные требования

1. **Python 3.11+**
2. **Tesseract OCR**
3. **OpenAI API ключ**

### Установка Tesseract

**macOS:**
```bash
brew install tesseract
```

**Ubuntu/Debian:**
```bash
sudo apt-get install tesseract-ocr
```

### Настройка проекта

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd accounting_assistant
```

2. **Создание виртуального окружения:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows
```

3. **Установка зависимостей:**
```bash
pip install -r requirements.txt
```

4. **Настройка переменных окружения:**
```bash
cp env_example.txt .env
# Отредактируйте .env файл, добавив ваш OpenAI API ключ
```

5. **Запуск сервера:**
```bash
cd back
python run.py
```

## 🌐 Использование

### Веб-интерфейс

Откройте браузер и перейдите по адресу: `http://localhost:8000`

### API Endpoints

- **Главная страница**: `GET /`
- **Загрузка документа**: `POST /upload`
- **Список документов**: `GET /documents`
- **Документы для проверки**: `GET /documents/pending`
- **Редактирование**: `PUT /documents/{id}/edit`
- **Скачивание**: `GET /documents/{id}/download`
- **Статистика**: `GET /statistics`
- **Экспорт отчетов**: `POST /reports/generate`

## 📊 Поддерживаемые типы документов

1. **Счет-фактура (Factura Fiscală)**
2. **Фискальный чек (Bon Fiscal)**
3. **Контракт (Contract)**
4. **Квитанция (Chitanță)**
5. **Накладная (Aviz de însoțire)**
6. **Счет (Factură)**
7. **Декларация (Declarație)**
8. **Другие документы**

## 🔧 Конфигурация

Основные настройки в файле `config.py`:

- `OPENAI_API_KEY`: Ключ API OpenAI
- `TESSERACT_PATH`: Путь к Tesseract
- `MAX_FILE_SIZE`: Максимальный размер файла (50MB)
- `UPLOADS_DIR`: Директория для загрузок
- `REPORTS_DIR`: Директория для отчетов

## 📈 Возможности AI

### Распознавание документов
- Автоматическое определение типа документа
- Извлечение ключевых полей
- Оценка уверенности распознавания

### Валидация данных
- Проверка обязательных полей
- Валидация форматов (IDNO, суммы, даты)
- Проверка соответствия стандартам Молдовы

### Обработка ошибок
- Автоматическое исправление типичных ошибок
- Предложения по улучшению
- Логирование всех операций

## 🗂 Структура файлов

```
accounting_assistant/
├── back/                    # Backend код
│   ├── main.py             # Основной FastAPI сервер
│   ├── config.py           # Конфигурация
│   ├── document_processor.py # Обработка документов
│   ├── document_storage.py # Хранение документов
│   ├── data_models.py      # Модели данных
│   ├── report_generator_v2.py # Генерация отчетов
│   ├── i18n.py            # Интернационализация
│   ├── templates/         # HTML шаблоны
│   │   └── index.html    # Главная страница
│   ├── uploads/          # Загруженные файлы
│   └── reports/          # Сгенерированные отчеты
├── requirements.txt       # Python зависимости
├── README.md             # Документация
└── .env                  # Переменные окружения
```

## 🌍 Поддерживаемые языки

- 🇷🇺 Русский (ru)
- 🇷🇴 Румынский (ro)

## 📝 Логирование

Все операции логируются в файл `logs/app.log` с уровнями:
- INFO: Обычные операции
- WARNING: Предупреждения
- ERROR: Ошибки

## 🔒 Безопасность

- Валидация загружаемых файлов
- Ограничение размера файлов
- Санитизация входных данных
- Логирование всех операций

## 🚀 Развертывание

### Docker (рекомендуется)

```bash
docker-compose up -d
```

### Ручное развертывание

```bash
# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
export OPENAI_API_KEY="your-api-key"

# Запуск сервера
python run.py
```

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи в `logs/app.log`
2. Убедитесь в корректности настроек
3. Проверьте доступность OpenAI API
4. Убедитесь в установке Tesseract

## 📄 Лицензия

MIT License - см. файл LICENSE для деталей.

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения
4. Создайте Pull Request

---

**Версия**: 2.0.0  
**Последнее обновление**: 2025-06-21  
**Автор**: AI Assistant Team 